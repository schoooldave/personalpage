# 本地测试说明

## 启动

在本目录运行：

```bash
npm run dev
```

默认使用 `http://localhost:3000/`。如果 3000 端口被其他本地进程占用，Next.js 会自动切换到 `3001`，以终端显示的地址为准。

## 主要页面

- `/`：个人星图首页与工作世界切换
- `/notes`：FMCG Field Notes 行业判断、案例证据与 AI 未来推演
- `/about`：独立个人介绍页
- `/cases/dms`：全国分销数据治理项目详情
- `/cases/sfa`：SFA 项目详情

## 建议验收顺序

1. 首页移动鼠标，确认星图微光响应。
2. 点击“进入工作世界”，确认过渡层和主题切换。
3. 打开 DMS / SFA 项目详情，检查结果信号和推进链路。
4. 从工作世界首页进入 `FMCG Field Notes`，确认判断、证据、AI 推演和公开边界均可读。
   - 桌面端滚动经过 02 时依次出现三条判断，没有跳动、突然空白或标签卡片。
   - 不移动鼠标也能读完标题、解释和三步关系；鼠标移动只产生轻微景深。
   - 键盘聚焦关系词时可以看到与鼠标相同的说明。
   - 02 结束后自然进入第一篇 Field Note，链接可打开 `/notes`。
   - 窄屏下三幕纵向排列，无粘滞滚动、遮挡或横向溢出。
   - 减少动态效果模式下三幕完整静态显示。
5. 切回个人世界，确认首页不显示行业思考区；打开“关于我”，检查经历路径、能力和联系区。
6. 点击 `schooldave@live.com`，确认邮件客户端入口正常。
7. 调整窗口宽度，检查移动端布局无横向溢出。

## 发布前待补

- 替换简历占位入口为正式 PDF。
- 最终确认邮箱、域名和公开内容边界。
- 完成正式部署前的域名与托管配置。
